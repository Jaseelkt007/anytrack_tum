# Phase 1 Python dependencies.
# Pinned to recent stable versions; bump as needed in later phases.

# --- Core ---
python-dotenv==1.0.1
requests==2.32.3

# --- Database (Postgres + pgvector via SQLAlchemy 2.0 async) ---
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
psycopg[binary]==3.2.3
alembic==1.14.0
pgvector==0.3.6

# --- M3 GitHub scraping ---
PyGithub==2.5.0

# --- M6 backend API ---
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4

# --- Phase 2 / M7 identity resolver (LLM arbitration via Gemini) ---
google-generativeai==0.8.3

# --- Job queue (Procrastinate, Postgres-backed; replaces APScheduler in v0.3) ---
procrastinate[psycopg2]==3.0.0
APScheduler==3.10.4  # legacy — only the disabled scheduler still imports this

# --- Phase 2 / M12-email — Resend digest delivery ---
resend==2.4.0

# --- Dev / testing ---
pytest==8.3.4
pytest-asyncio==0.24.0
ruff==0.8.4
