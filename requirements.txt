# Phase 1 Python dependencies.
# Pinned to recent stable versions; bump as needed in later phases.

# --- Core ---
neo4j==5.28.1
python-dotenv==1.0.1
requests==2.32.3

# --- M3 GitHub scraping ---
PyGithub==2.5.0

# --- M6 backend API ---
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4

# --- Phase 2 / M7 identity resolver (LLM arbitration via Gemini) ---
google-generativeai==0.8.3

# --- Dev / testing ---
pytest==8.3.4
ruff==0.8.4
